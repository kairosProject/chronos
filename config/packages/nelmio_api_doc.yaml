nelmio_api_doc:
    documentation:
        info:
            title: Chronos
            description: The kairos API
            version: 1.0.0-dev
    routes: # to filter documented routes
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
